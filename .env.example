VITE_XLAYER_NETWORK=testnet

# X Layer network details from OKX docs:
# testnet chain id: 1952 / 0x7A0
# mainnet chain id: 196 / 0xC4
#
# The current frontend submits the prediction as a zero-value X Layer proof
# transaction with the prediction payload in calldata. After deploying
# contracts/KnewBallCup.sol, add contract calls for registerFan,
# submitPrediction, resolveMatch, and claimBallIQ.
